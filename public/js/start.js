/* ============================================================
   start.js — Beginner Cyber Pathway (/start)
   Progress, badges, streaks, and motion. Runs alongside main.js
   (which handles the navbar + auth). The pathway itself is
   server-rendered by the worker; this enhances it per-user.
   ============================================================ */

'use strict';

// Progress wiring, gamification, and motion are added in later steps.
document.addEventListener('DOMContentLoaded', () => {
  const pathway = document.querySelector('.pathway');
  if (!pathway) return;
  // (enhancement logic to come)
});
